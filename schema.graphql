### This file was generated by Nexus Schema
### Do not make changes to this file directly


type Mutation {
  createReservation(dateFrom: String!, dateTo: String!, roomId: Int!, userId: Int!): Reservation!
  createRoom(price: Float!, roomNumber: Int!, roomType: RoomType!): Room!
  createUser(email: String!, firstName: String!, lastName: String!, role: Role!): User!
}

type Query {
  getReservation(reservationId: Int!): Reservation
  getReservations: [Reservation]
  getRoom(roomId: Int!): Room
  getRooms: [Room]
  getUser(userId: Int!): User
  getUsers: [User]
}

type Reservation {
  dateFrom: String!
  dateTo: String!
  id: ID!
  roomId: Int!
  userId: Int!
}

enum Role {
  CLERK
  GUEST
  MANAGER
}

type Room {
  id: ID!
  price: Float!
  roomNumber: Int!
  roomType: RoomType!
}

enum RoomType {
  DOUBLE
  EXCLUSIVE
  PRESIDENTIAL
  SINGLE
}

type User {
  email: String!
  firstName: String!
  id: ID!
  lastName: String!
  role: Role!
}